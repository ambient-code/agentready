name: tier1-attrs
description: "All Tier 1 attributes"
agentready_changes:
  align:
    enabled: true
    attributes:
      - agent_instructions
      - readme_structure
      - type_annotations
      - standard_layout
      - lock_files
